package seedu.addressbook.data.person;

/**
 * An interface in the addressbook which creates a printable string representation of Name, Phone, Email, and Address.
 */

public interface Printable {
	public String getPrintableString();
}
